# Ejercicios Avanzado

**A1.** ADR de 1 página: OneAgent vs OTel vs híbrido para un servicio real.

**A2.** Propón un metric event (nombre, ámbito por tag, por qué no basta Davis). No lo apliques en prod sin review.

**A3.** Notebook runbook con parámetros `env` + servicio: RED + top 5xx + logs allowlist.

**A4.** Lista labels K8s que **no** deben ser dimensiones de métrica.

**A5.** Diagrama de un workflow Problem → Slack **sin** payload. Scopes del token (nombres, no el secreto).

**A6.** Propuesta SLO de un journey (SLI, %, exclusiones, burn).

**A7.** Estimación cualitativa: qué pasa si logueas body JSON en cada request de prod.
